import { TranslateFuelPipe } from './translate-fuel.pipe';

describe('TranslateFuelPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFuelPipe();
    expect(pipe).toBeTruthy();
  });
});
