import { ShorterTextPipe } from './shorter-text.pipe';

describe('ShorterTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShorterTextPipe();
    expect(pipe).toBeTruthy();
  });
});
